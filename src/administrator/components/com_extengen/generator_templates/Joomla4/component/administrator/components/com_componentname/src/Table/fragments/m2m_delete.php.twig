
		// Delete the {{entityName|lower}} from the {{pivotTable}} junction table.
		$query->clear()
			->delete($db->quoteName('#__{{componentName|lower}}_{{pivotTable}}'))
			->where($db->quoteName('{{entityName|lower}}_id') . ' = :key')
			->bind(':key', $key, ParameterType::INTEGER);
		$db->setQuery($query);
		$db->execute();

