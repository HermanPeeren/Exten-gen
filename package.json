{
  "name": "exten-gen",
  "private": true,
  "type": "module",
  "description": "Browser tests for the component: node:test for the logic that needs no DOM, Cypress for what needs a running Joomla.",
  "scripts": {
    "test": "node --test tests/js/",
    "cypress": "cypress run",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^15.21.1"
  }
}
